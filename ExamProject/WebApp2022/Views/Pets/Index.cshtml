@using WebApp2022.Core.Models
@model IEnumerable<PetAllViewModel>
<div class="mb-5">
    <h3 class="text-center text-success">Do the search here</h3>
</div>

<div class="text-center row">
    @foreach (var product in Model)
    {
        <partial name="_AllPetDetailsAtAllPagePartial" model="product"/>
    }
</div>